import 'package:retrofit/retrofit.dart';
import 'package:dio/dio.dart';

part '${NAME}.g.dart';

@RestApi(baseUrl: '')
abstract class ${ServiceName} {
  factory ${ServiceName}(Dio dio, {String baseUrl}) = _${ServiceName};

  // @GET('/tasks')
  // Future<List<Task>> getTasks();
}